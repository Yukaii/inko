{
  "name": "inko",
  "version": "0.1.0",
  "devDependencies": {
    "@auth/core": "0.37.0",
    "concurrently": "^9.1.2",
    "typescript": "^5.8.2",
    "vite-plus": "catalog:"
  },
  "private": true,
  "scripts": {
    "build": "vp run --filter @inko/shared build && vp run --filter @inko/api build && vp run --filter @inko/web build",
    "dev": "concurrently \"vp run --filter @inko/api dev\" \"vp run --filter @inko/web dev\"",
    "db:migrate": "vp run --filter @inko/api migrate",
    "test": "vp test",
    "lint": "vp run --filter @inko/shared lint && vp run --filter @inko/api lint && vp run --filter @inko/web lint"
  },
  "packageManager": "pnpm@10.32.1"
}
