# Frontend

Frontend app: `apps/web` (Vite + React + TypeScript).

Common commands:

- `bun run --filter @inko/web dev`
- `bun run --filter @inko/web test`
- `bun run --filter @inko/web lint`
- `bun run --filter @inko/web build`
